trigger:
- dev
- test
- acc
- production


pool:
  vmImage: 'windows-latest'

extends:
  template: azure-pipelines-template.yml
  parameters:
    env: "test"
    target_store: "obfrontendtest"
    fa-token: $(fa_token)